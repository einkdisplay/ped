[package]
name = "fbink-sys"
version = "0.1.0"
edition = "2024"
publish = false
links = "fbink"
description = "Minimal Kindle FBInk FFI bindings for PED"
license = "GPL-3.0-or-later"

[build-dependencies]
bindgen = "0.72"
cc = "1"
