# Vendored FBInk (Kindle IMAGE subset)

Snapshot of https://github.com/NiLuJe/FBInk sources required to build
`fbink-sys` with FBINK_FOR_KINDLE + FBINK_MINIMAL + DRAW + IMAGE.

Compiled directly by `crates/fbink-sys/build.rs` (not FBInk Makefile).
See LICENSE for upstream terms.
